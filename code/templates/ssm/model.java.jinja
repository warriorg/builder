package {{meta.package}}.{{meta.module}}.model;

import lombok.Getter;
import lombok.Setter;

import com.dcjet.cs.base.model.BasicModel;
import javax.persistence.Column;
import javax.persistence.Id;
import javax.persistence.Table;
import java.io.Serializable;
import java.math.BigDecimal;
import java.util.Date;

import java.io.Serializable;

/**
 * {{meta.comment}}
 * 
 * generated by Generate 神码
 * @author builder
 * @date: {{meta.get_date()}}
 */
@Setter @Getter
@Table(name = "{{meta.table}}")
public class {{meta.name}} extends BasicModel implements Serializable {
    private static final long serialVersionUID = 1L;

    {% for field in meta.fields if not field.filter %}
    /**
     * {{field.comment}}
     */
    {%- if field.primary %}
    @Id
    {%- endif %}
    @Column(name = "{{field.column}}")
    private {{field.java_type}} {{field.name}};
    {% endfor %}
}